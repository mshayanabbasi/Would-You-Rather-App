const styles = theme => ({
    gridList: {
        justifyContent: 'space-between',
        minHeight: 'inherit',
        alignItems: 'center',
        width: 'inherit',
        display: 'flex'
    },
    
    gridListTile: {
        justifyContent: 'center',
        display: 'flex'
    }
})

export default styles